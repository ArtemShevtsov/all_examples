import { rentalPropertyTypeHelper } from 'rentals/helpers/rental-property-type-helper';
import { module, test } from 'qunit';

module('Unit | Helper | rental property type helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalPropertyTypeHelper([42]);
  assert.ok(result);
});
